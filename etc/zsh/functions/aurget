local aurdir=${SRCDEST:-~/tmp/aur}
mkdir -p $aurdir
if [ ! -f $aurdir/$1 ]; then
    git clone -q https://aur.archlinux.org/$1.git $aurdir/$1 || return $?
    cd $aurdir/$1 || return $?
else
    cd $aurdir/$1 || return $?
    git pull
fi
