# Various settings that inform the CLI and tools about where my projects live,
# how they're structured, and what kind of environment they're running in

export RACK_ENV=development
export RAILS_ENV=development

export PROJECT_DIR=${HOME}/code
