local aurdir=${SRCDEST:-~/tmp/aur}
mkdir -p $aurdir
wget -q https://aur.archlinux.org/packages/${1:0:2}/$1/$1.tar.gz -P $aurdir || return $?
bsdtar xf $aurdir/$1.tar.gz -C $aurdir || return $?
rm $aurdir/$1.tar.gz || return $?
cd $aurdir/$1 || return $?
